zerops:
  - setup: prerender
    build:
      base: nodejs@20
      buildCommands:
        - npm i
      deploy:
        - node_modules
        - server.js
    run:
      base: nodejs@20
      prepareCommands:
        - sudo apk add --update-cache chromium
        - sudo rm -rf /var/cache/apk/* /tmp/*
      envVariables:
        NODE_ENV: production
        MEMORY_CACHE: 1
      ports:
        - port: 3000
          httpSupport: true
      start: node server.js

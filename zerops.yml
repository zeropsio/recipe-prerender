zerops:
  - setup: prerender
    build:
      base: nodejs@20
      buildCommands:
        - npm i
      deploy:
        - node_modules
        - server.js
    run:
      base: nodejs@20
      prepareCommands:
        - apk add --update-cache chromium tini && rm -rf /var/cache/apk/* /tmp/*
      envVariables:
        NODE_ENV: production
      ports:
        - port: 3000
          httpSupport: true
      start: node server.js
